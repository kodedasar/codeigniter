{% extends "layouts/base.twig" %}

{% block title %}
	Change Password
{% endblock %}

{% block content %}
	<div class="login-box">
		<div class="login-logo">
			<a href="{{ base_url() }}">
				{{ getenv('CI_NAME') }}
			</a>
		</div>

		<div class="login-box-body">
			<p class="login-box-msg">{{ lang('change_password_heading') }}</p>

			{{ form_open('auth/change_password') }}

			<div class="form-group has-feedback">
				{{ form_input(old_password) }}
				<span class="glyphicon glyphicon-lock form-control-feedback"></span>
			</div>

			<div class="form-group has-feedback">
				{{ form_input(new_password) }}
				<span class="glyphicon glyphicon-lock form-control-feedback"></span>
			</div>

			<div class="form-group has-feedback">
				{{ form_input(new_password_confirm) }}
				<span class="glyphicon glyphicon-lock form-control-feedback"></span>
			</div>

			<div class="text-danger">
				{{ message|raw }}
			</div>

			{{ form_input(user_id) }}

			{{ form_submit('submit', lang('change_password_submit_btn'), 'class="btn btn-primary btn-block btn-flat"') }}

			{{ form_close() }}

		</div>

	</div>
{% endblock %}
